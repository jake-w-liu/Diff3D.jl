# Test fixtures

- `AllHalfValues.exr` — from the openexr-images repository
  (AcademySoftwareFoundation/openexr-images, BSD-3-Clause). A 256x256 PIZ-
  compressed EXR whose RGB pixels enumerate every 16-bit half value, used as
  exact decoder-independent ground truth for the EXR PIZ decoder.
